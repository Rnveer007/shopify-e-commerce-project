<div class="cx-block-media cx-media-{{ block.id }}" style="margin-bottom: {{ block.settings.mb }};">
  
  <div class="cx-media-inner" style="border-radius: {{ block.settings.radius }}px;">
    {%- comment -%} Check if Link exists {%- endcomment -%}
    {% if block.settings.image_link != blank %}
      <a href="{{ block.settings.image_link }}" class="cx-media-card-link">
    {% endif %}

    {% if block.settings.image_desktop != blank %}
      <picture>
        {% if block.settings.image_mobile != blank %}
          <source media="(max-width: 767px)" srcset="{{ block.settings.image_mobile | image_url: width: 800 }}">
        {% endif %}
        <img 
          src="{{ block.settings.image_desktop | image_url: width: 2000 }}" 
          alt="{{ block.settings.image_desktop.alt | escape }}"
          loading="lazy"
          class="cx-media-img"
        >
      </picture>
    {% else %}
      {{ 'lifestyle-2' | placeholder_svg_tag: 'cx-placeholder' }}
    {% endif %}

    {% if block.settings.image_link != blank %}
      </a>
    {% endif %}
  </div>

</div>

<style>
  .cx-media-{{ block.id }} .cx-media-inner {
    overflow: hidden;
    width: 100%;
    line-height: 0;
  }
  .cx-media-card-link {
    display: block;
    width: 100%;
    text-decoration: none;
  }
  .cx-media-img {
    width: 100%;
    height: auto;
    object-fit: cover;
    display: block;
    transition: opacity 0.3s ease;
  }
  /* Optional: Halki si opacity change jab link par hover ho */
  .cx-media-card-link:hover .cx-media-img {
    opacity: 0.9;
  }
  .cx-placeholder { 
    background: #eee; 
    width: 100%; 
    height: 300px; 
  }
</style>

{% schema %}
{
  "name": "CX Media Card",
  "settings": [
    { "type": "header", "content": "Media" },
    { "type": "image_picker", "id": "image_desktop", "label": "Image (Desktop)" },
    { "type": "image_picker", "id": "image_mobile", "label": "Image (Mobile)" },
    { "type": "url", "id": "image_link", "label": "Image Link (Optional)" },
    { "type": "header", "content": "Style" },
    { "type": "range", "id": "radius", "min": 0, "max": 40, "step": 2, "unit": "px", "label": "Corner Roundness", "default": 12 },
    {
      "type": "select",
      "id": "mb",
      "label": "Margin Bottom",
      "options": [
        { "value": "0px", "label": "None" },
        { "value": "20px", "label": "Small" },
        { "value": "40px", "label": "Medium" },
        { "value": "60px", "label": "Large" }
      ],
      "default": "20px"
    }
  ],
  "presets": [
    { "name": "CX Media Card" }
  ]
}
{% endschema %}